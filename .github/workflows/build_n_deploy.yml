name: Trigger Deploy Script
on:
  push:
     branches:
       - deploy
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Run deploy script
        uses: HydPy/hydpyconf2019@ci-cd
        with:
          command: |
            bundle -v
          host: ${{ secrets.HOST }}
          user: ${{ secrets.USER }}
          key: ${{ secrets.KEY }}
